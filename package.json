{
    "dependencies": {
        "concurrently": "^7.4.0"
    },
    "scripts": {
        "start": "concurrently \"cd front/build; npm start\" \"cd back; npm start\"",
        "build": "cd front; npm build",
        "test": "react-scripts test",
        "eject": "react-scripts eject"
    }
}
