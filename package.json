{
    "dependencies": {
        "concurrently": "^7.4.0"
    },
    "scripts": {
        "start": "concurrently \"cd\" \"cd .\\front; npm start\" \"cd .\\back; npm start\"",
        "build": "cd front; npm run build",
        "test": "react-scripts test",
        "eject": "react-scripts eject",
        "osef": "osef"
    }
}
